---
 - hosts: all
   roles:
      - download
      - gui
      - security
      - vxlan
      - dns

 - hosts: nfs
   roles:
      - nfsserver

 - hosts: dbserver
   roles:
      - common
      - nfsclient
