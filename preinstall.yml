---
 - hosts: all
   roles:
      - ssh
      - centos2oel
      - security
      - vxlan
      - dns

 - hosts: nfs
   roles:
      - nfsserver

 - hosts: dbserver
   roles:
      - common
      - nfsclient
