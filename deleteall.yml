---
 - hosts: dbserver
   connection: local
   tasks:
     - name: delete container dbservers
       shell: "{{ DELETE_CMD }} {{ INSTANCE_ID }}"
       ignore_errors: yes

 - hosts: nfs
   connection: local
   tasks:
     - name: delete container nfs
       shell: "{{ DELETE_CMD }} {{ INSTANCE_ID }}" 
       ignore_errors: yes
