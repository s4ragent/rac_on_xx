---
####  stop
 - hosts: all:!storage
   gather_facts: False
   connection: local
   roles:
     - stop

 - hosts: storage
   environment:
     SYSTEMD_IGNORE_CHROOT: 1 
   roles:
     - stop-storage-service
 
 - hosts: storage
   gather_facts: False
   connection: local
   roles:
     - stop
