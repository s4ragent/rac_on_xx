---
 - hosts: all:!nfs
   connection: local
   tasks:
     - name: stop container without nfs
       shell: "{{ STOP_CMD }} {{ INSTANCE_ID }} {{ STOP_CMD_OPS }}" 
       ignore_errors: yes

 - hosts: nfs
   tasks:
     - name: "stop nfs-server"
       become: yes
       service: name=nfs-server.service state=stopped
       ignore_errors: yes

 - hosts: nfs
   connection: local
   tasks:
     - name: stop container nfs
       shell: "{{ STOP_CMD }} {{ INSTANCE_ID }} {{ STOP_CMD_OPS }}" 
       ignore_errors: yes
