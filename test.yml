---
  - hosts: 
      - localhost
    connection: local
    environment:
      SYSTEMD_IGNORE_CHROOT: 1
    become: yes
    strategy: debug
    tasks:
      - pause: 
      - systemd: name=network.service state=enabled