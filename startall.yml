---
 - hosts: nfs
   connection: local
   tasks:
     - name: start container nfs
       shell: {{ START_CMD }} {{ INSTANCE_ID }} 
       ignore_errors: yes

 - hosts: dbserver
   connection: local
   tasks:
     - name: start container dbservers
       shell: {{ START_CMD }} {{ INSTANCE_ID }} 
       ignore_errors: yes
