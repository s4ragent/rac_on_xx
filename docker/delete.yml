---
 - hosts: dbserver
   tasks:
     - name: delete container dbservers
       local_action: shell docker rm -f {{ CONTAINER_NAME }} 
       ignore_errors: yes
 - hosts: nfs
   tasks:
     - name: delete container nfs
       local_action: shell docker rm -f {{ CONTAINER_NAME }} 
       ignore_errors: yes